# Fixture catalog

This directory stores **references** to upstream generated fixtures (name, version pin, SHA-256) and local fault scripts.

It must not copy Schema field layouts, Envelope definitions, or a second Codec. When `UPSTREAM-GENERATED-CONTRACT-API-MAP` is unpublished, `index.json` records that block and keeps `hashes` empty.
